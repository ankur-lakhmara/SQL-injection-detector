{
  "name": "sql-injection-detector",
  "version": "1.0.0",
  "description": "a package to detect the sql injection on pattern basis",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "express": "^4.18.2"
  }
}
